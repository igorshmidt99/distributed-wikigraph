spring:
  queue:
    amount: 4
    prefix: "queue.worker-"
  rabbitmq:
    host: "localhost"
    port: 5672
    password: "secret"
    admin: "myuser"
    addresses: "amqp://myuser:secret@localhost"
  application:
    name: "router"
    show-sql: true
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.PostgreSQLDialect"
        format_sql: true
  datasource:
#    url: jdbc:h2:mem:mydb
#    username: sa
#    password: password
#    driverClassName: org.h2.Driver
    url: "jdbc:postgresql://172.17.0.3:5432/postgres"
    username: "postgres"
    password: "postgres"
    driverClassName: org.postgresql.Driver